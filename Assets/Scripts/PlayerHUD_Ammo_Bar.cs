using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


public class PlayerHUD_Ammo_Bar : MonoBehaviour
{
    public GameObject player;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*if (player == null)
            gameObject.GetComponent<Text>().text = "Ammo: " + GameObject.Find("PlayerSimulatorV2").GetComponent<PlayerStatus>().ammo;

        else
            gameObject.GetComponent<Text>().text = "Ammo: " + player.GetComponent<PlayerStatus>().ammo;*/
    }
}
